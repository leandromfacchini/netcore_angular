@{
    ViewData["Title"] = "Ocorreu um erro";
}

<h1 class="text-danger">@ViewData["Title"]</h1>
<p class="text-danger">
    Ocorreu um erro, tente novamente ou contate um
    administrador.
</p>

@{

    if (ViewData["MensagemErro"] != null)
    {
        <br />
        <h2 class="text-danger">@ViewData["MensagemErro"]</h2>
    }
}